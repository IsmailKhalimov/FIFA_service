# Пример для Streamlit Cloud / другого хостинга с встроенными секретами.
# Скопируйте содержимое в Secrets в панели хостинга (не коммитьте secrets.toml с паролями).

DB_HOST = "localhost"
DB_PORT = "5432"
DB_NAME = "football_service"
DB_USER = "postgres"
DB_PASSWORD = "Logitech1524"

# Опционально для продакшн-PostgreSQL с SSL:
# DB_SSLMODE = "require"
